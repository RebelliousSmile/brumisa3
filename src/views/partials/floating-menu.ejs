<!-- Menu flottant latéral (desktop uniquement) -->
<div class="fixed left-4 md:left-8 top-1/2 -translate-y-1/2 flex flex-col gap-4 z-40 hidden md:flex"
     x-data="{ showTooltip: false }">
    <div class="relative">
        <a href="#" @click="$dispatch('toggleDonation')"
           @mouseenter="showTooltip = true" @mouseleave="showTooltip = false"
           class="w-10 h-10 bg-blue-600/20 border border-blue-500/30 rounded-full flex items-center justify-center hover:border-blue-500 transition-all duration-standard">
            <i class="ra ra-crowned-heart text-blue-400 text-xl"></i>
        </a>
        <div x-show="showTooltip"
             x-transition:enter="transition ease-out duration-200"
             x-transition:enter-start="opacity-0 translate-x-2"
             x-transition:enter-end="opacity-100 translate-x-0"
             class="absolute left-full ml-4 top-1/2 -translate-y-1/2 bg-noir-charbon border border-gris-fonce px-3 py-2 rounded-lg whitespace-nowrap">
            <span class="text-sm text-white">Soutenir le projet</span>
            <div class="absolute -left-2 top-1/2 -translate-y-1/2 w-0 h-0 border-t-8 border-t-transparent border-r-8 border-r-gris-fonce border-b-8 border-b-transparent"></div>
        </div>
    </div>
</div>

<!-- Version mobile en bas -->
<div class="fixed bottom-4 left-1/2 -translate-x-1/2 flex flex-row gap-3 z-40 md:hidden bg-noir-charbon/70 backdrop-blur-sm px-4 py-3 rounded-full border border-gris-fonce">
    <a href="#" @click="$dispatch('toggleDonation')"
       class="w-10 h-10 bg-blue-600/20 border border-blue-500/30 rounded-full flex items-center justify-center hover:border-blue-500 transition-all duration-standard">
        <i class="ra ra-crowned-heart text-blue-400 text-xl"></i>
    </a>
</div>