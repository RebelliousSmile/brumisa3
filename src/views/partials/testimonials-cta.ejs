<!-- Témoignages CTA -->
<section class="relative z-10 px-4 mb-16">
    <div class="max-w-6xl mx-auto">
        <!-- Background CTA style avec couleur générique -->
        <div class="relative rounded-2xl overflow-hidden bg-gradient-to-br from-gray-900 via-blue-900 to-generique p-8 mb-12">
            <div class="relative z-10">
                <h2 class="text-2xl md:text-3xl font-bold text-white mb-8 text-center font-display">
                    Ce que disent nos utilisateurs
                </h2>
                
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    <template x-for="temoignage in temoignages.slice(0, 3)" :key="temoignage.id">
                        <div class="bg-gray-900/50 backdrop-blur-sm rounded-lg p-6 border border-generique/20">
                            <div class="flex items-center mb-4">
                                <div class="flex text-yellow-400" x-html="'★'.repeat(temoignage.note) + '☆'.repeat(5-temoignage.note)"></div>
                                <span class="ml-2 text-sm text-gray-400" x-text="temoignage.note + '/5'"></span>
                            </div>
                            
                            <h4 class="font-semibold text-white mb-3" x-text="temoignage.titre" x-show="temoignage.titre"></h4>
                            <p class="text-gray-300 mb-4" x-text="temoignage.contenu"></p>
                            
                            <div class="flex items-center justify-between text-sm">
                                <span class="text-gray-400" x-text="temoignage.nom"></span>
                                <span x-text="temoignage.systeme_utilise" x-show="temoignage.systeme_utilise" class="bg-gray-700 text-gray-300 px-2 py-1 rounded-full border border-gray-600"></span>
                            </div>
                        </div>
                    </template>
                </div>
                
                <!-- CTA pour ajouter un témoignage -->
                <div class="text-center mt-8">
                    <%- include('../components/ui-button', {
                        text: 'Laisser un témoignage',
                        variant: 'primary',
                        size: 'lg',
                        action: 'afficherFormulaireTemoignage = !afficherFormulaireTemoignage',
                        gameSystem: locals.gameSystem || '',
                        additionalClasses: 'font-display'
                    }) %>
                </div>
            </div>
        </div>
    </div>
</section>