<%
// CTA Système v2.0 - Migré vers ui-cta avec support thèmes
// Compatibilité maintenue avec les anciens paramètres

// Récupération des paramètres passés
const {
    titreCTA = 'Rejoignez l\'aventure',
    descriptionCTA = 'Créez votre compte et commencez à jouer',
    gradient = 'from-gray-900 to-black',
    couleurTexte = 'text-gray-100',
    couleurTexteBouton = 'bg-blue-600 hover:bg-blue-700',
    couleurTexteBoutonSecondaire = 'border-gray-400 text-gray-400 hover:bg-gray-400',
    gameSystem = '',
    systemTheme = null
} = locals;

// Mappage des anciens paramètres vers les nouveaux
const ctaButtons = [
    {
        text: 'Créer un compte gratuit',
        href: '/inscription',
        variant: 'primary',
        size: 'xl',
        customClass: `inline-flex items-center justify-center px-8 py-4 text-xl font-semibold rounded-xl transition-all ${couleurTexteBouton} text-white`
    },
    {
        text: 'Se connecter',
        href: '/connexion',
        variant: 'outline',
        size: 'xl',
        customClass: `inline-flex items-center justify-center px-8 py-4 text-xl font-semibold rounded-xl transition-all ${couleurTexteBoutonSecondaire} hover:text-gray-900`
    }
];

// Configuration pour l'include
const ctaConfig = {
    title: titreCTA,
    subtitle: descriptionCTA,
    backgroundType: 'gradient',
    gradient: gradient,
    gameSystem: gameSystem,
    systemTheme: systemTheme,
    buttons: ctaButtons,
    centered: true,
    padding: 'py-16 px-4',
    customClass: couleurTexte
};
%>

<!-- CTA Système v2.0 -->
<%- include('../components/ui-cta', ctaConfig) %>