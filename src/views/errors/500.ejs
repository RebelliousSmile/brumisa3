<!-- Page 500 -->
<div class="min-h-screen flex items-center justify-center bg-gray-900">
    <div class="max-w-md w-full text-center px-6">
        <!-- Icône d'erreur -->
        <div class="mb-8">
            <div class="mx-auto w-24 h-24 bg-blue-600/20 rounded-full flex items-center justify-center border border-blue-500/30">
                <i class="ra ra-broken-skull text-blue-400 text-4xl"></i>
            </div>
        </div>

        <!-- Titre et message -->
        <h1 class="text-6xl font-bold text-blue-400 mb-4 font-display">500</h1>
        <h2 class="text-2xl font-semibold text-white mb-4">Erreur serveur</h2>
        <p class="text-gray-400 mb-8">
            Une erreur interne s'est produite sur le serveur. Notre équipe a été notifiée et travaille à résoudre le problème.
        </p>


        <!-- Informations de débogage (en mode développement) -->
        <% if (process.env.NODE_ENV === 'development' && typeof erreur !== 'undefined') { %>
        <div class="mt-8 p-4 bg-gray-100 rounded-lg text-left">
            <h3 class="font-semibold text-gray-800 mb-2">Détails de l'erreur (dev uniquement):</h3>
            <pre class="text-xs text-gray-600 whitespace-pre-wrap overflow-auto max-h-32"><%= erreur.stack || erreur.message || erreur %></pre>
        </div>
        <% } %>

    </div>
</div>