<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- typeof titre !== 'undefined' ? titre : 'brumisa3.fr' %></title>
    
    <!-- Meta tags -->
    <meta name="description" content="Créateur de fiches de personnages JDR immersives">
    <meta name="author" content="RebelliousSmile">
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
    
    <!-- Tailwind CSS -->
    <link href="/css/style.css" rel="stylesheet">
    
    <!-- Google Fonts pour la charte graphique -->
    <link href="https://fonts.googleapis.com/css2?family=Source+Serif+4:ital,wght@0,400;0,500;0,700;1,400&family=Crimson+Text:ital,wght@0,400;1,400&family=Playfair+Display:ital,wght@0,400;1,400&family=Roboto+Condensed:wght@400;700&family=Spectral:ital,wght@0,400;1,400&display=swap" rel="stylesheet">
    
    <!-- Gaming Icon Fonts -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/rpg-awesome/0.2.0/css/rpg-awesome.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Adobe Fonts - Shackleton -->
    <link rel="stylesheet" href="https://use.typekit.net/vpm3oax.css">
    
    <!-- Données serveur pour Alpine.js -->
    <script>
        window.APP_DATA = {
            utilisateur: <%- JSON.stringify(locals.utilisateur || null) %>,
            systemes: <%- JSON.stringify(locals.systemes || {}) %>,
            csrfToken: '<%= locals.csrfToken || "" %>'
        };
    </script>
    
    <!-- Alpine.js defer pour permettre le chargement des composants d'abord -->
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
    
    <!-- Stores et composants avant Alpine.js -->
    <script src="/js/app.js"></script>
    <script src="/js/services/PersonnageService.js"></script>
    <script src="/js/services/PdfService.js"></script>
    <script src="/js/components/PersonnageComponent.js"></script>
    <script src="/js/components/AuthComponent.js"></script>
    <script src="/js/components/TableauBordComponent.js"></script>
    <script src="/js/components/PageAccueilComponent.js"></script>
</head>
<body class="bg-gray-900 min-h-screen">
    <!-- Telex Topline -->
    <div class="bg-gradient-to-r from-purple-600 to-blue-600 text-white py-2 px-4 text-center relative overflow-hidden">
        <div class="absolute inset-0 bg-black/10"></div>
        <div class="relative z-10 flex items-center justify-center space-x-2 text-sm font-display">
            <i class="ra ra-lightning-bolt text-yellow-300 animate-pulse"></i>
            <span class="hidden sm:inline">📢</span>
            <span>Nouvelle fonctionnalité : Export PDF amélioré pour tous les systèmes !</span>
            <i class="ra ra-lightning-bolt text-yellow-300 animate-pulse"></i>
        </div>
    </div>
    
    <!-- Navigation -->
    <nav class="bg-gray-900 border-b border-gray-800 sticky top-0 z-40" x-data="window.AlpineComponents.navigationMobile()">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between items-center h-16">
                <!-- Logo -->
                <div class="flex items-center">
                    <a href="/" class="text-xl font-bold flex items-center font-display">
                        <i class="ra ra-brain-freeze text-blue-400 text-2xl mr-2"></i>
                        <span class="hidden sm:inline text-blue-400">brumisa3.fr</span>
                    </a>
                </div>
                
                <!-- Game Links -->
                <div class="hidden md:flex items-center space-x-8 font-display">
                    <a href="/personnages?systeme=monsterhearts" class="flex items-center text-gray-300 hover:text-purple-400 transition-colors">
                        <i class="ra ra-heartburn text-purple-400 mr-2"></i>
                        <span class="text-sm">Monsterhearts</span>
                    </a>
                    <a href="/personnages?systeme=engrenages" class="flex items-center text-gray-300 hover:text-emerald-400 transition-colors">
                        <i class="ra ra-candle text-emerald-400 mr-2"></i>
                        <span class="text-sm">Engrenages</span>
                    </a>
                    <a href="/personnages?systeme=metro2033" class="flex items-center text-gray-300 hover:text-red-400 transition-colors">
                        <i class="ra ra-pills text-red-400 mr-2"></i>
                        <span class="text-sm">Metro 2033</span>
                    </a>
                    <a href="/personnages?systeme=mistengine" class="flex items-center text-gray-300 hover:text-pink-400 transition-colors">
                        <i class="ra ra-ocarina text-pink-400 mr-2"></i>
                        <span class="text-sm">Mist Engine</span>
                    </a>
                </div>
                
                <!-- User Menu -->
                <div class="flex items-center">
                    <% if (locals.utilisateur) { %>
                        
                        <!-- User Icon -->
                        <div class="relative" x-data="window.AlpineComponents.menuUtilisateur()">
                            <button @click="basculer()" class="w-10 h-10 bg-blue-600/20 rounded-full flex items-center justify-center border border-blue-500/30 hover:border-blue-400 transition-colors">
                                <% if (locals.utilisateur.avatar_url) { %>
                                    <img src="<%= locals.utilisateur.avatar_url %>" alt="Avatar" class="w-full h-full rounded-full object-cover">
                                <% } else { %>
                                    <div class="w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center">
                                        <span class="text-white font-semibold text-sm">
                                            <%= (locals.utilisateur.nom || 'U').charAt(0).toUpperCase() %>
                                        </span>
                                    </div>
                                <% } %>
                            </button>
                            
                            <div x-show="ouvert" @click.away="fermer()" x-transition 
                                 class="absolute right-0 mt-2 w-48 bg-gray-800 border border-gray-700 rounded-lg shadow-lg py-2 z-50">
                                <div class="px-4 py-2 border-b border-gray-700">
                                    <p class="text-sm font-medium text-white"><%= locals.utilisateur.nom %></p>
                                    <p class="text-xs text-gray-400"><%= locals.utilisateur.role %></p>
                                </div>
                                <% if (locals.utilisateur.role === 'UTILISATEUR') { %>
                                <button @click="$dispatch('open-role-elevation')" class="block w-full text-left px-4 py-2 text-sm text-blue-400 hover:bg-gray-700">
                                    Débloquer Premium/Admin
                                </button>
                                <% } %>
                                <a href="/parametres" class="block px-4 py-2 text-sm text-gray-300 hover:bg-gray-700">Paramètres</a>
                                <a href="/mes-documents" class="block px-4 py-2 text-sm text-gray-300 hover:bg-gray-700">Mes documents</a>
                                <% if (locals.utilisateur.role !== 'UTILISATEUR') { %>
                                <a href="/admin" class="block px-4 py-2 text-sm text-gray-300 hover:bg-gray-700">Administration</a>
                                <% } %>
                                <hr class="my-1 border-gray-700">
                                <button @click="deconnecter()" class="block w-full text-left px-4 py-2 text-sm text-red-400 hover:bg-gray-700">Se déconnecter</button>
                            </div>
                        </div>
                    <% } else { %>
                        <a href="/connexion" class="w-10 h-10 bg-blue-600/20 rounded-full flex items-center justify-center border border-blue-500/30 hover:border-blue-400 transition-colors">
                            <i class="ra ra-player-king text-blue-400 text-lg"></i>
                        </a>
                    <% } %>
                </div>
                </div>
                
                <!-- Menu mobile button -->
                <div class="md:hidden">
                    <button @click="basculerMenu()" class="text-gray-600 hover:text-gray-900">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                        </svg>
                    </button>
                </div>
            </div>
            
            <!-- Menu mobile dropdown -->
            <div x-show="menuOuvert" x-transition class="md:hidden border-t border-gray-200 py-2">
                <% if (locals.utilisateur) { %>
                    <a @click="naviguer('/')" class="block px-4 py-2 text-gray-600 hover:bg-gray-100">Accueil</a>
                    <a @click="naviguer('/mes-documents')" class="block px-4 py-2 text-gray-600 hover:bg-gray-100">Mes documents</a>
                    <hr class="my-2">
                    <div class="px-4 py-2">
                        <p class="text-sm font-medium text-gray-900"><%= locals.utilisateur.nom %></p>
                        <p class="text-xs text-gray-500">Rôle: <%= locals.utilisateur.role %></p>
                    </div>
                    <% if (locals.utilisateur.role === 'UTILISATEUR') { %>
                    <button @click="$dispatch('open-role-elevation')" class="block w-full text-left px-4 py-2 text-sm text-blue-600 hover:bg-blue-50">
                        Débloquer Premium/Admin
                    </button>
                    <% } %>
                    <% if (locals.utilisateur.role !== 'UTILISATEUR') { %>
                    <a @click="naviguer('/admin')" class="block px-4 py-2 text-gray-600 hover:bg-gray-100">Administration</a>
                    <% } %>
                    <button @click="$store.app.requeteApi('/auth/deconnexion', {method: 'POST'}).then(() => location.href = '/connexion')" 
                            class="block w-full text-left px-4 py-2 text-red-600 hover:bg-red-50">
                        Se déconnecter
                    </button>
                <% } else { %>
                    <a @click="naviguer('/connexion')" class="block px-4 py-2 text-gray-600 hover:bg-gray-100">Connexion</a>
                    <a @click="naviguer('/inscription')" class="block px-4 py-2 text-blue-600 hover:bg-blue-50">Créer un compte</a>
                <% } %>
            </div>
        </div>
    </nav>
    
    <!-- Indicateur hors ligne -->
    <div x-data="window.AlpineComponents.indicateurHorsLigne()" x-show="isOffline" 
         class="bg-yellow-500 text-white text-center py-2 text-sm">
        <span>📱 Mode hors ligne - Vos modifications sont sauvegardées localement</span>
    </div>
    
    <!-- Indicateur changements non sauvegardés -->
    <div x-data="window.AlpineComponents.indicateurHorsLigne()" x-show="hasUnsavedChanges" 
         class="bg-blue-500 text-white text-center py-1 text-xs">
        <span>💾 Changements non sauvegardés</span>
    </div>
    
    <!-- Messages flash -->
    <div class="fixed top-20 right-4 z-50 space-y-2" x-data="window.AlpineComponents.messagesFlash()">
        <template x-for="message in messages" :key="message.id">
            <div class="bg-white rounded-lg shadow-lg p-4 min-w-80 max-w-md" 
                 :class="{
                     'border-l-4 border-green-500': message.type === 'succes',
                     'border-l-4 border-red-500': message.type === 'erreur',
                     'border-l-4 border-yellow-500': message.type === 'avertissement',
                     'border-l-4 border-blue-500': message.type === 'info'
                 }"
                 x-transition:enter="transform ease-out duration-300 transition"
                 x-transition:enter-start="translate-x-full opacity-0"
                 x-transition:enter-end="translate-x-0 opacity-100"
                 x-transition:leave="transition ease-in duration-300"
                 x-transition:leave-start="opacity-100"
                 x-transition:leave-end="opacity-0">
                <div class="flex items-start">
                    <div class="flex-shrink-0">
                        <span class="text-lg" x-text="obtenirIcone(message.type)"></span>
                    </div>
                    <div class="ml-3 flex-1">
                        <p class="text-sm font-medium text-gray-900" x-text="message.texte"></p>
                    </div>
                    <div class="ml-4 flex-shrink-0">
                        <button @click="supprimerMessage(message.id)" class="text-gray-400 hover:text-gray-600">
                            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                            </svg>
                        </button>
                    </div>
                </div>
            </div>
        </template>
    </div>
    
    <!-- Modal élévation de rôle -->
    <% if (locals.utilisateur && locals.utilisateur.role === 'UTILISATEUR') { %>
    <div x-data="window.AlpineComponents.elevationRole()" 
         @open-role-elevation.window="ouvrir()"
         x-show="ouvert" 
         class="fixed inset-0 z-50 overflow-y-auto">
        <div class="flex items-center justify-center min-h-screen px-4">
            <div class="fixed inset-0 bg-black opacity-50" @click="fermer()"></div>
            <div class="relative bg-white rounded-lg p-6 w-full max-w-md">
                <h3 class="text-lg font-medium text-gray-900 mb-4">Code d'accès Premium/Admin</h3>
                <input x-ref="inputCode" 
                       x-model="codeAcces" 
                       @keydown="gererTouche($event)"
                       type="text" 
                       placeholder="Code à 6 chiffres" 
                       maxlength="6"
                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                <div class="flex justify-end space-x-3 mt-4">
                    <button @click="fermer()" class="px-4 py-2 text-gray-600 hover:text-gray-800">Annuler</button>
                    <button @click="verifier()" 
                            :disabled="enTraitement || codeAcces.length !== 6"
                            class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 disabled:opacity-50">
                        <span x-show="!enTraitement">Vérifier</span>
                        <span x-show="enTraitement">Vérification...</span>
                    </button>
                </div>
            </div>
        </div>
    </div>
    <% } %>
    
    <!-- Menu flottant latéral -->
    <%- include('../partials/floating-menu') %>
    
    <!-- Bouton retour en haut -->
    <%- include('../partials/back-to-top') %>
    
    <!-- Contenu principal -->
    <main class="min-h-screen">
        <%- body %>
    </main>
    
    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-8 mt-12">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex flex-col md:flex-row justify-between items-center">
                <!-- Copyright et crédits -->
                <div class="text-center md:text-left mb-4 md:mb-0">
                    <p>&copy; 2025 brumisa3.fr. Créé avec ❤️ pour la communauté JDR.</p>
                    <p class="text-sm text-gray-400 mt-1">
                        Développé par <a href="https://scriptami.com" target="_blank" rel="noopener" class="text-purple-400 hover:text-purple-300 transition-colors">scriptami.com</a>
                    </p>
                </div>
                
                <!-- Liens légaux et RSS -->
                <div class="flex space-x-6 text-sm">
                    <a href="/mentions-legales" class="text-gray-400 hover:text-white transition-colors">
                        Mentions légales
                    </a>
                    <a href="/cgu" class="text-gray-400 hover:text-white transition-colors">
                        CGU
                    </a>
                    <a href="/api/actualites/rss" class="text-gray-400 hover:text-purple-400 transition-colors flex items-center">
                        <svg class="w-4 h-4 inline mr-1" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M5 3a1 1 0 000 2c5.523 0 10 4.477 10 10a1 1 0 102 0C17 8.373 11.627 3 5 3z"/>
                            <path d="M4 9a1 1 0 011-1 7 7 0 017 7 1 1 0 11-2 0 5 5 0 00-5-5 1 1 0 01-1-1zM3 15a2 2 0 114 0 2 2 0 01-4 0z"/>
                        </svg>
                        RSS
                    </a>
                </div>
            </div>
        </div>
    </footer>
</body>
</html>