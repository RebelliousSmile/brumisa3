<!-- Layout Metro 2033 - Post-apocalyptique brutal -->
<div class="min-h-screen bg-gray-900 relative overflow-hidden">
    <!-- Background Effects -->
    <div class="absolute inset-0 bg-gradient-to-br from-red-900/20 via-red-800/30 to-gray-900"></div>
    <div class="absolute inset-0">
        <div class="absolute top-20 left-10 w-72 h-72 bg-red-500/10 rounded-full filter blur-3xl"></div>
        <div class="absolute bottom-20 right-10 w-96 h-96 bg-orange-500/10 rounded-full filter blur-3xl"></div>
    </div>

    <!-- Navigation principale -->
    <%- include('../layouts/principal') %>

    <!-- Fil d'Ariane -->
    <%- include('../partials/breadcrumb', { systeme: 'metro2033' }) %>

    <!-- Contenu principal -->
    <main class="relative z-10">
        <%- body %>
    </main>

    <!-- Footer système -->
    <%- include('../partials/footer-systeme', { systeme: 'metro2033' }) %>
</div>

<style>
:root {
    --system-primary: #dc2626;
    --system-secondary: #ef4444;
    --system-accent: #f97316;
    --system-bg-from: #991b1b;
    --system-bg-to: #1f2937;
}
</style>