<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.titre || 'Brumisa3 - Générateur PDF JDR' %></title>
    
    <!-- Meta tags SEO -->
    <meta name="description" content="<%= locals.description || 'Créateur de fiches de personnages JDR avec génération PDF' %>">
    <meta name="keywords" content="jdr, jeu de rôle, fiche personnage, pdf, monsterhearts, engrenages">
    <meta name="author" content="Brumisa3">
    
    <!-- Open Graph -->
    <meta property="og:title" content="<%= locals.titre || 'Brumisa3 - Générateur PDF JDR' %>">
    <meta property="og:description" content="<%= locals.description || 'Créateur de fiches de personnages JDR avec génération PDF' %>">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= process.env.BASE_URL || 'https://brumisater' %>">
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Cinzel:wght@400;500;600&display=swap" rel="stylesheet">
    
    <!-- CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/rpg-awesome@0.2.0/css/rpg-awesome.min.css">
    
    <!-- Alpine.js -->
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
    
    <style>
        .font-display { font-family: 'Cinzel', serif; }
        .font-body { font-family: 'Inter', sans-serif; }
        
        /* Variables CSS système - seront surchargées par les layouts enfants */
        :root {
            --system-primary: #3b82f6;
            --system-secondary: #60a5fa; 
            --system-accent: #ec4899;
            --system-bg-from: #1e40af;
            --system-bg-to: #1f2937;
        }
    </style>
</head>
<body class="font-body">
    <%- body %>
</body>
</html>