<!-- Landing Page - Dark Gaming Theme -->
<div class="min-h-screen bg-black relative overflow-hidden" 
     x-data="window.AlpineComponents.pageAccueil()">
    
    <!-- Hero Section -->
    <header class="relative pt-20 pb-16 px-4 overflow-hidden">
        <!-- Background Effects - Hero gradient limité à la hero section -->
        <div class="absolute inset-0 bg-gradient-to-br from-gray-900 via-blue-900 to-generique"></div>
        <div class="absolute inset-0">
            <div class="absolute top-20 left-10 w-72 h-72 bg-generique/20 rounded-full filter blur-3xl"></div>
            <div class="absolute bottom-20 right-10 w-96 h-96 bg-purple-500/20 rounded-full filter blur-3xl"></div>
        </div>
        <div class="max-w-7xl mx-auto">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
                <!-- Character Image - À gauche -->
                <div class="relative lg:order-first order-first">
                    <div class="relative max-w-md mx-auto lg:ml-0">
                        <!-- Hero Glow Effect -->
                        <div class="absolute inset-0 bg-gradient-to-r from-generique/30 to-purple-500/30 rounded-full filter blur-3xl"></div>
                        
                        <!-- Character -->
                        <img src="/images/transhumans-bueno.png" 
                             alt="Personnage cyberpunk" 
                             class="relative z-10 w-full h-auto hero-character-filter">
                    </div>
                </div>
                
                <!-- Content - À droite -->
                <div class="text-center lg:text-left lg:order-last relative z-10">
                    <h1 class="text-4xl md:text-6xl font-bold text-white mb-6 leading-tight font-display">
                        N'EMBARQUEZ PAS DANS LA BRUME
                        <span class="text-generique">
                            LES MAINS VIDES
                        </span>
                    </h1>
                    <p class="text-xl md:text-2xl text-gray-300 leading-relaxed font-serif">
                        Transformez vos héros en légendes épiques dans des fiches de personnages immersives.
                        Créez vos aventuriers impeccables et préparez-vous pour des expériences épiques.
                    </p>
                </div>
            </div>
        </div>
    </header>

    <!-- Stats Section -->
    <section class="relative z-10 px-4 my-16">
        <div class="max-w-6xl mx-auto">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <!-- Stat 1 - Utilisateurs inscrits -->
                <div class="text-center">
                    <div class="w-16 h-16 bg-gray-900 rounded-full flex items-center justify-center mx-auto mb-4 border border-generique">
                        <i class="ra ra-player-king text-generique text-2xl"></i>
                    </div>
                    <div class="text-3xl md:text-4xl font-bold text-white mb-2" x-text="formaterNombre(statistiques?.nb_utilisateurs_inscrits) || '...'"></div>
                    <div class="text-generique font-medium font-display">UTILISATEURS</div>
                    <div class="text-gray-400 text-sm mt-1">Joueuses inscrites créant des personnages 
épiques et partageant leurs aventures.</div>
                </div>
                
                <!-- Stat 2 - Contenus ouverts ce mois -->
                <div class="text-center">
                    <div class="w-16 h-16 bg-gray-900 rounded-full flex items-center justify-center mx-auto mb-4 border border-generique">
                        <i class="fa-solid fa-eye text-generique text-2xl"></i>
                    </div>
                    <div class="text-3xl md:text-4xl font-bold text-white mb-2" x-text="formaterNombre(statistiques?.nb_contenus_ouverts_mois) || '...'"></div>
                    <div class="text-generique font-medium font-display">VUES CE MOIS</div>
                    <div class="text-gray-400 text-sm mt-1">PDFs consultés et téléchargés 
par la communauté ce mois-ci.</div>
                </div>
                
                <!-- Stat 3 - PDFs stockés -->
                <div class="text-center">
                    <div class="w-16 h-16 bg-gray-900 rounded-full flex items-center justify-center mx-auto mb-4 border border-generique">
                        <i class="fa-solid fa-scroll text-generique text-2xl"></i>
                    </div>
                    <div class="text-3xl md:text-4xl font-bold text-white mb-2" x-text="formaterNombre(statistiques?.nb_pdfs_stockes) || '...'"></div>
                    <div class="text-generique font-medium font-display">PDFS STOCKÉS</div>
                    <div class="text-gray-400 text-sm mt-1">Fiches de personnages et aides de jeu sauvegardées .</div>
                </div>
            </div>
        </div>
    </section>
    
    <!-- Game Systems Section -->
    <section class="relative z-10 px-4 mb-16">
        <div class="max-w-6xl mx-auto">
            <!-- Game World Background -->
            <div class="relative rounded-2xl overflow-hidden bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 border border-gray-600 p-8 mb-12">
                <div class="relative z-10">
                    <div class="text-center mb-8">
                        <h2 class="text-2xl md:text-3xl font-bold text-white mb-4 font-display">
                            Choisissez votre système de jeu
                        </h2>
                        <p class="text-gray-300 max-w-2xl mx-auto">
                            Chaque système possède son propre design thématique et ses mécaniques uniques pour créer des fiches immersives.
                        </p>
                    </div>
                    
                    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                        <!-- Colonne principale (2/3) -->
                        <div class="lg:col-span-2">
                            <div class="grid grid-cols-1 gap-6">
                                <% if (locals.systemCards && locals.systemCards.mainColumn) { %>
                                    <% systemCards.mainColumn.forEach(card => { 
                                        const safeCard = card || {};
                                        const safeClasses = safeCard.classes || { bg: 'bg-gray-600/20', border: 'border-gray-500/30', text: 'text-gray-400', badgeBg: 'bg-gray-500/20', badgeBorder: 'border-gray-500/30' };
                                        const safeIcon = safeCard.icon || 'ra-dice';
                                        const safeVersion = safeCard.version || 'Version inconnue';
                                    %>
                                        <div data-carte-systeme 
                                             class="group bg-gray-900 rounded-lg p-6 border border-gray-700 transition-all hover:border-gray-600">
                                            <div class="flex items-start">
                                                <div class="w-16 h-16 <%= safeClasses.bg %> rounded-full flex items-center justify-center flex-shrink-0 <%= safeClasses.border %> mr-4">
                                                    <i class="ra <%= safeIcon %> <%= safeClasses.text %> text-2xl"></i>
                                                </div>
                                                <div class="flex-grow">
                                                    <h3 class="text-lg font-bold text-white mb-1"><%= safeCard.nom %></h3>
                                                    <p class="text-sm text-gray-400 mb-3">
                                                        <%= safeCard.description %>
                                                    </p>
                                                    
                                                    <% if (safeCard.univers && safeCard.univers.length > 0) { %>
                                                        <div class="flex flex-wrap gap-2 mb-3">
                                                            <% safeCard.univers.forEach(univers => { %>
                                                                <a href="/systeme/<%= safeCard.code %>/<%= univers.code %>" 
                                                                   class="inline-flex items-center px-3 py-1 text-xs rounded-full bg-gray-800 hover:bg-gray-700 text-gray-300 hover:text-white border border-gray-700 hover:border-gray-600 transition-all">
                                                                    <%= univers.nom %>
                                                                </a>
                                                            <% }); %>
                                                        </div>
                                                    <% } else { %>
                                                        <a href="/systeme/<%= safeCard.code %>" 
                                                           class="inline-flex items-center px-3 py-1 text-xs rounded-full bg-gray-800 hover:bg-gray-700 text-gray-300 hover:text-white border border-gray-700 hover:border-gray-600 transition-all">
                                                            Accéder au système
                                                        </a>
                                                    <% } %>
                                                    
                                                    <span class="inline-block text-xs <%= safeClasses.badgeBg %> <%= safeClasses.text %> px-3 py-1 rounded-full <%= safeClasses.badgeBorder %> font-display"><%= safeVersion %></span>
                                                </div>
                                            </div>
                                        </div>
                                    <% }); %>
                                <% } %>
                            </div>
                        </div>
                        
                        <!-- Colonne secondaire (1/3) -->
                        <div class="lg:col-span-1">
                            <div class="grid grid-cols-1 gap-6">
                                <% if (locals.systemCards && locals.systemCards.secondColumn) { %>
                                    <% systemCards.secondColumn.forEach(card => { 
                                        const safeCard = card || {};
                                        const safeClasses = safeCard.classes || { bg: 'bg-gray-600/20', border: 'border-gray-500/30', text: 'text-gray-400', badgeBg: 'bg-gray-500/20', badgeBorder: 'border-gray-500/30' };
                                        const safeIcon = safeCard.icon || 'ra-dice';
                                        const safeVersion = safeCard.version || 'Version inconnue';
                                    %>
                                        <div data-carte-systeme 
                                             class="group bg-gray-900 rounded-lg p-6 border border-gray-700 transition-all hover:border-gray-600">
                                            <div class="text-center">
                                                <div class="w-16 h-16 <%= safeClasses.bg %> rounded-full flex items-center justify-center mx-auto mb-4 <%= safeClasses.border %>">
                                                    <i class="ra <%= safeIcon %> <%= safeClasses.text %> text-2xl"></i>
                                                </div>
                                                <h3 class="text-lg font-bold text-white mb-2"><%= safeCard.nom %></h3>
                                                <p class="text-sm text-gray-400 mb-4">
                                                    <%= safeCard.description %>
                                                </p>
                                                
                                                <% if (safeCard.univers && safeCard.univers.length > 0) { %>
                                                    <div class="flex flex-col gap-2 mb-4">
                                                        <% safeCard.univers.forEach(univers => { %>
                                                            <a href="/systeme/<%= safeCard.code %>/<%= univers.code %>" 
                                                               class="inline-flex items-center justify-center px-3 py-1 text-xs rounded-full bg-gray-800 hover:bg-gray-700 text-gray-300 hover:text-white border border-gray-700 hover:border-gray-600 transition-all">
                                                                <%= univers.nom %>
                                                            </a>
                                                        <% }); %>
                                                    </div>
                                                <% } else { %>
                                                    <a href="/systeme/<%= safeCard.code %>" 
                                                       class="inline-flex items-center justify-center px-3 py-1 text-xs rounded-full bg-gray-800 hover:bg-gray-700 text-gray-300 hover:text-white border border-gray-700 hover:border-gray-600 transition-all mb-4">
                                                        Accéder au système
                                                    </a>
                                                <% } %>
                                                
                                                <span class="inline-block text-xs <%= safeClasses.badgeBg %> <%= safeClasses.text %> px-3 py-1 rounded-full <%= safeClasses.badgeBorder %> font-display"><%= safeVersion %></span>
                                            </div>
                                        </div>
                                    <% }); %>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>


    
    <!-- PDFs Récents -->
    <section class="relative z-10 px-4 mb-16">
        <div class="max-w-6xl mx-auto">
            <h2 class="text-2xl md:text-3xl font-bold text-white mb-8 text-center font-display">
                Dernières Créations Partagées
            </h2>
            
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <template x-for="pdf in pdfsRecents" :key="pdf.id">
                    <div class="bg-gray-900  rounded-lg p-6 border border-gray-700 hover:border-gray-600 transition-all ">
                        <div class="flex items-start justify-between mb-3">
                            <h3 class="font-semibold text-white text-sm font-serif" x-text="pdf.personnage_nom"></h3>
                            <span class="text-xs bg-gray-700 text-gray-300 px-2 py-1 rounded-full border border-gray-600 font-display" x-text="pdf.systeme_jeu"></span>
                        </div>
                        <p class="text-xs text-gray-400 mb-3 font-serif">
                            par <span x-text="pdf.auteur_nom"></span>
                        </p>
                        <div class="flex items-center justify-between text-xs text-gray-500 font-serif">
                            <span x-text="tempsDepuis(pdf.date_creation)"></span>
                            <button @click="pdfService.telecharger(pdf.id)" class="inline-flex items-center px-3 py-1 bg-brand-violet text-white rounded-full text-xs font-medium hover:bg-brand-violet-dark transition-colors">
                                <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20"><path d="M9 3a1 1 0 012 0v7.586l2.293-2.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 111.414-1.414L9 10.586V3z"></path><path d="M3 17a1 1 0 011-1h12a1 1 0 011 1v1a1 1 0 01-1 1H4a1 1 0 01-1-1v-1z"></path></svg>
                                Télécharger
                            </button>
                        </div>
                    </div>
                </template>
                
                <!-- PDFs factices si aucun PDF récent -->
                <div x-show="!pdfsRecents || pdfsRecents.length === 0" class="col-span-full">
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                        <!-- PDF exemple 1 -->
                        <div class="bg-gray-900  rounded-lg p-6 border border-gray-700 hover:border-gray-600 transition-all ">
                            <div class="flex items-start justify-between mb-3">
                                <h3 class="font-semibold text-white text-sm font-serif">Véra Blackthorne</h3>
                                <span class="text-xs bg-gray-700 text-gray-300 px-2 py-1 rounded-full border border-gray-600 font-display">Monsterhearts</span>
                            </div>
                            <p class="text-xs text-gray-400 mb-3 font-serif">
                                par <span>Alex M.</span>
                            </p>
                            <div class="flex items-center justify-between text-xs text-gray-500 font-serif">
                                <span>15/01/2024</span>
                                <span>23 téléch.</span>
                            </div>
                        </div>
                        
                        <!-- PDF exemple 2 -->
                        <div class="bg-gray-900  rounded-lg p-6 border border-gray-700 hover:border-gray-600 transition-all ">
                            <div class="flex items-start justify-between mb-3">
                                <h3 class="font-semibold text-white text-sm font-serif">Capitaine Hartwell</h3>
                                <span class="text-xs bg-emerald-500 text-emerald-400 px-2 py-1 rounded-full border border-emerald-500 font-display">Engrenages</span>
                            </div>
                            <p class="text-xs text-gray-400 mb-3 font-serif">
                                par <span>Marie L.</span>
                            </p>
                            <div class="flex items-center justify-between text-xs text-gray-500 font-serif">
                                <span>12/01/2024</span>
                                <span>31 téléch.</span>
                            </div>
                        </div>
                        
                        <!-- PDF exemple 3 -->
                        <div class="bg-gray-900  rounded-lg p-6 border border-gray-700 hover:border-gray-600 transition-all ">
                            <div class="flex items-start justify-between mb-3">
                                <h3 class="font-semibold text-white text-sm font-serif">Dmitri Volkov</h3>
                                <span class="text-xs bg-red-500 text-red-400 px-2 py-1 rounded-full border border-red-500 font-display">Metro 2033</span>
                            </div>
                            <p class="text-xs text-gray-400 mb-3 font-serif">
                                par <span>Pierre K.</span>
                            </p>
                            <div class="flex items-center justify-between text-xs text-gray-500 font-serif">
                                <span>10/01/2024</span>
                                <span>18 téléch.</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Newsletter + Rejoignez l'aventure -->
    <section class="relative z-10 px-4 mb-16">
        <div class="max-w-6xl mx-auto">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                
                <!-- Newsletter -->
                <div class="bg-gray-900  rounded-lg p-6 border border-gray-700">
                    <h3 class="text-xl font-bold text-white mb-4 font-display">
                        Restez informé
                    </h3>
                    <p class="text-gray-300 mb-6 font-serif">
                        Soyez averti des nouvelles fonctionnalités pour les 5 jeux de la plateforme : Monsterhearts, Engrenages, Metro 2033, Mist Engine et Zombiology.
                    </p>
                    
                    <form @submit.prevent="inscrireNewsletter()" x-show="!newsletterInscrit">
                        <div class="space-y-4">
                            <input 
                                type="email" 
                                x-model="emailNewsletter"
                                placeholder="votre@email.com"
                                required
                                class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:border-generique focus:outline-none font-serif">
                            <input 
                                type="text" 
                                x-model="nomNewsletter"
                                placeholder="Votre nom (optionnel)"
                                class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:border-generique focus:outline-none font-serif">
                            <button 
                                type="submit"
                                :disabled="chargementNewsletter"
                                class="w-full bg-gradient-to-r from-generique to-blue-600 text-white py-3 px-6 rounded-lg font-medium hover:from-blue-600 hover:to-generique disabled:bg-gray-600 disabled:text-gray-400 disabled:cursor-not-allowed transition-all font-display">
                                <span x-show="!chargementNewsletter">S'inscrire à la newsletter</span>
                                <span x-show="chargementNewsletter">Inscription...</span>
                            </button>
                        </div>
                    </form>
                    
                    <div x-show="newsletterInscrit" class="text-center">
                        <div class="text-green-400 mb-2 flex items-center justify-center">
                            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                            </svg>
                            Inscription réussie !
                        </div>
                        <p class="text-gray-400 font-serif">Vous recevrez les prochaines actualités par email.</p>
                    </div>
                    
                    <div class="mt-6 pt-4 border-t border-gray-700">
                        <p class="text-gray-400 text-center font-serif">
                            <span x-text="statistiques.nb_abonnes_newsletter"></span> abonnés
                        </p>
                    </div>
                </div>

                <!-- Rejoignez l'aventure -->
                <div class="bg-gray-900  rounded-lg p-6 border border-gray-700">
                    <h3 class="text-xl font-bold text-white mb-4 font-display">
                        Rejoignez l'aventure
                    </h3>
                    <p class="text-gray-300 mb-6 font-serif">
                        Créez un compte pour débloquer toutes les fonctionnalités :
                    </p>
                    
                    <!-- Avantages -->
                    <dl class="space-y-4 mb-6">
                        <div>
                            <dt class="text-white font-medium font-serif">Sauvegarde automatique</dt>
                            <dd class="text-gray-400 text-sm font-serif">Vos brouillons sont conservés en sécurité</dd>
                        </div>
                        <div>
                            <dt class="text-white font-medium font-serif">PDFs permanents</dt>
                            <dd class="text-gray-400 text-sm font-serif">Accès illimité à toutes vos créations</dd>
                        </div>
                        <div>
                            <dt class="text-white font-medium font-serif">Partage communauté</dt>
                            <dd class="text-gray-400 text-sm font-serif">Inspirez d'autres joueurs avec vos créations</dd>
                        </div>
                    </dl>
                    
                    <% if (!locals.utilisateur) { %>
                    <div class="text-center">
                        <a href="/inscription" class="inline-block w-full bg-gradient-to-r from-generique to-blue-600 text-white py-3 px-6 rounded-lg font-medium hover:from-blue-600 hover:to-generique transition-all font-display">
                            Créer un compte gratuit
                        </a>
                    </div>
                    
                    <div class="mt-6 pt-4 border-t border-gray-700">
                        <p class="text-gray-400 text-center font-serif">
                            <a href="/connexion" class="text-generique hover:text-blue-600 transition-colors">
                                J'ai déjà un compte
                            </a>
                        </p>
                    </div>
                    <% } else { %>
                    <div class="text-center">
                        <p class="text-green-400 mb-4 font-serif">Vous êtes déjà connecté !</p>
                        <a href="/personnages" class="inline-block w-full bg-gray-700 text-white py-3 px-6 rounded-lg font-medium hover:bg-gray-600 transition-all font-display">
                            Créer un personnage
                        </a>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>
    </section>

    <%- include('partials/testimonials-cta') %>

    <!-- Formulaire de témoignage -->
    <section class="relative z-10 px-4 mb-16" x-show="afficherFormulaireTemoignage">
        <div class="max-w-2xl mx-auto bg-gray-900  rounded-lg p-8 border border-gray-700">
            <h3 class="text-xl font-bold text-white mb-6">Partagez votre expérience</h3>
            
            <form @submit.prevent="ajouterTemoignage()">
                <div class="space-y-6">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <input 
                            type="text" 
                            x-model="nouveauTemoignage.nom"
                            placeholder="Votre nom *"
                            required
                            class="px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:border-green-500 focus:outline-none">
                        <input 
                            type="email" 
                            x-model="nouveauTemoignage.email"
                            placeholder="Email (optionnel)"
                            class="px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:border-green-500 focus:outline-none">
                    </div>
                    
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <select x-model="nouveauTemoignage.systeme_utilise" class="px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white focus:border-green-500 focus:outline-none">
                            <option value="">Système utilisé (optionnel)</option>
                            <option value="monsterhearts">Monsterhearts</option>
                            <option value="engrenages">Engrenages & Sortilèges</option>
                            <option value="metro2033">Metro 2033</option>
                            <option value="mistengine">Mist Engine</option>
                            <option value="zombiology">Zombiology</option>
                        </select>
                        <select x-model="nouveauTemoignage.note" required class="px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white focus:border-green-500 focus:outline-none">
                            <option value="">Note *</option>
                            <option value="5">5 étoiles (5/5)</option>
                            <option value="4">4 étoiles (4/5)</option>
                            <option value="3">3 étoiles (3/5)</option>
                            <option value="2">2 étoiles (2/5)</option>
                            <option value="1">1 étoile (1/5)</option>
                        </select>
                    </div>
                    
                    <input 
                        type="text" 
                        x-model="nouveauTemoignage.titre"
                        placeholder="Titre de votre témoignage (optionnel)"
                        class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:border-green-500 focus:outline-none">
                    
                    <textarea 
                        x-model="nouveauTemoignage.contenu"
                        placeholder="Votre témoignage *"
                        required
                        rows="4"
                        class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:border-green-500 focus:outline-none resize-none"></textarea>
                    
                    <div class="flex items-center justify-between">
                        <button 
                            type="button"
                            @click="afficherFormulaireTemoignage = false"
                            class="text-gray-400 hover:text-gray-300 transition-colors">
                            Annuler
                        </button>
                        <button 
                            type="submit"
                            :disabled="chargementTemoignage"
                            class="bg-gradient-to-r from-generique to-blue-600 text-white px-8 py-3 rounded-lg font-medium hover:from-blue-600 hover:to-generique disabled:bg-gray-600 disabled:text-gray-400 disabled:cursor-not-allowed transition-all">
                            <span x-show="!chargementTemoignage">Envoyer</span>
                            <span x-show="chargementTemoignage">Envoi...</span>
                        </button>
                    </div>
                </div>
            </form>
            
            <p class="text-gray-400 mt-6">
                Votre témoignage sera examiné avant publication pour s'assurer qu'il respecte nos conditions d'utilisation.
            </p>
        </div>
    </section>


    <!-- Features -->
    <section class="relative z-10 px-4 mb-16">
        <div class="max-w-6xl mx-auto">
            <h2 class="text-2xl md:text-3xl font-bold text-white mb-12 text-center font-display">
                Pourquoi choisir notre générateur ?
            </h2>
            
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div class="text-center bg-gray-900  rounded-lg p-8 border border-gray-700">
                    <div class="w-16 h-16 bg-gray-900 rounded-full flex items-center justify-center mx-auto mb-6 border border-generique">
                        <i class="fa-solid fa-mobile-alt text-generique text-2xl"></i>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-4">Mobile-First</h3>
                    <p class="text-gray-300">
                        Optimisé pour créer vos fiches sur mobile et tablette avec une interface intuitive
                    </p>
                </div>
                
                <div class="text-center bg-gray-900  rounded-lg p-8 border border-gray-700">
                    <div class="w-16 h-16 bg-gray-900 rounded-full flex items-center justify-center mx-auto mb-6 border border-generique">
                        <i class="fa-solid fa-palette text-generique text-2xl"></i>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-4">Thèmes Uniques</h3>
                    <p class="text-gray-300">
                        Chaque système possède son propre design visuel immersif et ses couleurs thématiques
                    </p>
                </div>
                
                <div class="text-center bg-gray-900  rounded-lg p-8 border border-gray-700">
                    <div class="w-16 h-16 bg-gray-900 rounded-full flex items-center justify-center mx-auto mb-6 border border-generique">
                        <i class="fa-solid fa-bolt text-generique text-2xl"></i>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-4">Partage Facile</h3>
                    <p class="text-gray-300">
                        Partagez vos fiches instantanément via liens directs, QR codes ou export PDF
                    </p>
                </div>
            </div>
        </div>
    </section>
</div>

<!-- CSS pour animations -->
<style>
@keyframes fade-in-up {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.animate-fade-in-up {
    animation: fade-in-up 0.6s ease-out forwards;
    opacity: 0;
}

/* Filter pour adapter l'image aux couleurs du site */
.hero-character-filter {
    filter: 
        hue-rotate(280deg)     /* Ajuste vers les violets du site */
        saturate(0.7)          /* Réduit la saturation */
        brightness(0.8)        /* Assombrit pour s'adapter au thème sombre */
        contrast(1.2);         /* Augmente le contraste */
    transition: all 0.3s ease-in-out;
}

/* Animation hover pour l'image */
.hero-character-filter:hover {
    filter: 
        hue-rotate(280deg)
        saturate(0.9)
        brightness(0.9)
        contrast(1.3);
    transform: scale(1.05);
}
</style>